#include <stdio.h>

int main() {

    // Relational Operators include these 

    // == To check if equal
    // >= Greater than or equal to
    // <= smaller than or equal to
    // < smaller than
    // > greater than
    // != not equal to


    // Logical Operators
    
    //  && AND operator - gives true only if both sides return true 
    // || OR operator - works if either sides give true 
    // ! NOT operator - works only if both sides are false just read it as Not True

     
    // For else - if ladder 

    // if{
    // // Statements
    // }
    // else if{
    // // Statements
    // }
    // else{
    // // Statements
    // }

    // we can use as many as we want else if statements

//     Priority Operator
//  1st         !
//  2nd         *, /, %
//  3rd         +, -
//  4th         <>, <=, >=
//  5th         ==, !=
//  6th         &&
//  7th         ||
//  8th         =

// shorthand conditional operator would go like this 

// somecondition ? expression-if-true : expression-if-false
// Here "?" and ":" are called Ternary Operators

    return 0;
}