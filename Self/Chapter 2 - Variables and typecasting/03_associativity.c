#include <stdio.h>

int main()
{
    // int a = 3;
    // int b = 6;
    // int c = 9;

    // printf("The value is %d", a*b/c); // The * has left to right operator precedence so it will be evaluvated first and with that if we keep / first instead of * this would evaluvate / first because this has the same operator precedence.

    // printf("The value is %d", 3*b/2*c+7*a);
 
    // 3*b/2*c + 7*a
    // 3*b/2*c + 21
    // 18/2*c + 21
    // 9*c + 21
    // 81 + 21
    // 102

    int x = 2;
    int y = 3;
    int z = 3;
    int k = 1;
    printf("%d",3*x/y - z+k);

    // 3 * x / y – z + k
    // 6 / 3 - 3 + 1
    // 2 - 3 + 1
    // -1 + 1
    // 0

    int a=1; int b = a;
    char dt = "21 dec 2020";
    int v = 3*3;

    

    return 0;
}